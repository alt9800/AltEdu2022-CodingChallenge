function setup() {
  createCanvas(400, 400);
}

function draw() {
  noFill();
  stroke(0, 0, 0);
  rect(width / 2 -120, width / 2 -90 , 240,180);
  
  stroke(255, 0, 0);
  fill(255,0,0);  
  circle(width / 2, height / 2, 90);
}
